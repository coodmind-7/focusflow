# FocusFlow — 项目架构文档

> 一款基于任务标签的计时与时间分配统计 Android 应用

---

## 1. 整体架构

采用 **Clean Architecture + MVVM** 分层设计，模块间依赖由外向内：

```
┌─────────────────────────────────────────────────────┐
│                   Presentation                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ Compose  │  │ NavHost  │  │  ViewModel       │  │
│  │ Screens  │◄─┤  Router  │◄─┤  (StateFlow)     │  │
│  └──────────┘  └──────────┘  └────────┬─────────┘  │
├───────────────────────────────────────┼─────────────┤
│                   Domain              │             │
│  ┌─────────────┐  ┌───────────────┐   │             │
│  │  UseCases   │──┤  Repository   │   │             │
│  │  (业务逻辑)  │  │  Interfaces   │   │             │
│  └─────────────┘  └───────────────┘   │             │
├───────────────────────────────────────┼─────────────┤
│                   Data                │             │
│  ┌──────────┐  ┌──────────┐  ┌───────┴──────────┐  │
│  │  Room DB │  │DataStore │  │ Repository Impl  │  │
│  └──────────┘  └──────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────┘
```

## 2. 模块划分

| 模块 | 职责 | 关键技术 |
|------|------|----------|
| **app** | 入口、DI 装配 | Hilt + Application |
| **core:ui** | 公共组件、主题、设计令牌 | Material3 + Compose |
| **core:data** | 数据库、持久化 | Room + DataStore |
| **feature:timer** | 正/倒计时核心 | Foreground Service |
| **feature:task** | 任务标签 CRUD | Room DAO |
| **feature:stats** | 统计计算与图表 | 自定义 Compose Canvas |

## 3. 核心数据流

以一次计时为例：

```
用户点击"开始"
  → ViewModel 调用 StartTimerUseCase
    → 创建 TimerSession(taskId, mode, startTime)
    → 启动 ForegroundService（保证后台不被杀）
  → 用户点击"结束"
    → Service 回调 StopTimerUseCase
    → 计算 elapsed = now - startTime
    → 写入 Room: TimerRecord(date, duration, taskId)
    → StatsEngine 重算当日占比
    → UI 通过 StateFlow 自动刷新饼图
```

## 4. 数据模型

### TaskLabel — 任务标签

```kotlin
@Entity
data class TaskLabel(
    @PrimaryKey val id: String,   // UUID
    val name: String,             // "学习"、"运动"
    val color: Long,              // 标签颜色
    val icon: String,             // 图标标识
    val sortOrder: Int
)
```

### TimerRecord — 计时记录

```kotlin
@Entity
data class TimerRecord(
    @PrimaryKey val id: String,
    val taskId: String,           // 关联任务标签
    val startTime: Long,          // epoch millis
    val duration: Long,           // 秒
    val mode: TimerMode,          // COUNT_UP / COUNT_DOWN
    val targetDuration: Long?,    // 倒计时目标（秒），正计时为 null
    val date: String              // yyyy-MM-dd，方便按日查询
)
```

## 5. UI 设计

### 设计理念

**克制、聚焦、轻量。** 核心目标：最少的操作步数到达计时状态。

### 色彩系统

```
主色：     #4F6EF7（沉静蓝紫）  — 专注、可信赖
背景：     #F8F9FC（浅灰白）    — 减少视觉疲劳
卡片：     #FFFFFF             — 干净分层
文字主：   #1C1E24             — 高对比度
文字辅：   #8E919A             — 弱化次要信息
强调色：   #FF6B6B（计时中）    — 仅计时进行中使用
```

### 屏幕结构（3 屏 + 1 浮层）

```
┌─────────────────────────┐
│  ┌───────────────────┐  │
│  │    计时器主页      │  │  ← 默认首页，核心交互
│  │  · 大号时间数字    │  │
│  │  · 任务标签横滑   │  │
│  │  · 开始/停止按钮  │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │    统计页          │  │  ← 饼图 + 日/周切换
│  │  · 时间占比饼图    │  │
│  │  · 各任务时长列表  │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │    任务管理页      │  │  ← 标签增删改
│  │  · 标签列表       │  │
│  └───────────────────┘  │
│  ════════════════════   │
│  底部导航栏              │
└─────────────────────────┘
```

### 各页面设计要点

#### 计时器主页（Hero Screen）

```
┌──────────────────────────────┐
│                              │
│        ◉  学习               │  ← 可横滑切换的任务 Chip，带颜色圆点
│                              │
│        ┌──────────┐         │
│        │  25:43   │         │  ← 大号等宽数字，字号 48sp+
│        │  / 30:00 │         │  ← 倒计时模式显示目标
│        └──────────┘         │
│                              │
│     ┌──────────────┐        │
│     │  ▷  /   ■   │        │  ← 一个大圆形按钮，开始/暂停
│     └──────────────┘        │
│                              │
│   正计时 ○——● 倒计时         │  ← 模式切换 toggle
│                              │
│  ── 今日 ────────────────── │
│  ☕ 运动   1.2h  ██████░░ 40%│  ← 今日时间条
│  📖 学习   1.8h  █████████ 60%│
│                              │
└──────────────────────────────┘
```

关键交互：
- **任务标签横滑 Chip 栏**：预置 3-5 个常用标签，左右滑动选择，最后一项是"+ 新建"
- **大圆按钮**：视觉占比最大的元素，点击即开始/暂停，无多余确认
- **今日摘要**：计时器下方用紧凑的横向进度条展示今日各任务占比

#### 统计页

```
┌──────────────────────────────┐
│  今日 · 本周 · 本月          │  ← 三档时间范围切换
│                              │
│         ┌──────┐            │
│         │ 饼图  │            │  ← 环形图（donut chart）
│         │donut │            │     中心显示总计 "3.0h"
│         └──────┘            │
│                              │
│  ☕ 运动    1.2h   40%       │  ← 图例列表
│  📖 学习    1.8h   60%       │
│                              │
└──────────────────────────────┘
```

饼图使用环形图（donut chart），中心留白放置总时长数字。

#### 任务管理页

简洁列表 + FAB 新增。每项左滑删除 / 长按拖拽排序。每个任务可设颜色标签（预设 8 色盘）。

### 动效建议

| 场景 | 动效 | 用意 |
|------|------|------|
| 开始计时 | 圆按钮 pulse 缩放 + 背景色渐变 | 仪式感，"进入专注" |
| 计时进行中 | 秒数跳动，冒号呼吸闪烁 | 感知时间流动 |
| 计时结束 | 数字弹跳 + 震动反馈 | 明确的完成信号 |
| 饼图出现 | 扇区依次展开动画 | 数据可视化更生动 |
| 页面切换 | 共享元素过渡 | 自然连贯 |

## 6. 技术栈

| 层 | 选型 |
|----|------|
| UI | Jetpack Compose + Material3 |
| 导航 | Navigation Compose |
| 状态管理 | ViewModel + StateFlow |
| DI | Hilt |
| 数据库 | Room |
| 偏好存储 | DataStore |
| 后台计时 | ForegroundService + Notification |
| 图表 | 自定义 Canvas |
| 最低 SDK | API 26 (Android 8.0) |

## 7. 推荐开发顺序

1. **项目骨架**：Hilt + Navigation + Room 建表
2. **任务标签模块**：CRUD 完整流程
3. **计时器核心**：ForegroundService + 正/倒计时逻辑
4. **统计模块**：数据聚合 + 饼图绘制
5. **UI 打磨**：动效、主题、暗色模式适配
